# SOURCE OF TRUTH: pyproject.toml ([project.dependencies]).
# This file is a thin shim so `pip install -r requirements.txt` keeps working —
# it installs the project in editable mode. pip routes through pyproject, so there
# is no second list to drift.
#
# Modern path: `uv sync`, or `pip install -e .` directly. This is the lean install
# for the `console` and `none` UI tiers (ADR 0010).
#
# requirements-core.txt is kept as a readable, tier-scoped reference; the live
# dependency list lives in pyproject.toml.
-e .
