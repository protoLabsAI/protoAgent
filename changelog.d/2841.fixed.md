- **Strip page chrome from `fetch_url` HTML responses (#2841).** `_extract_text_from_html` now removes `<header>` elements and ARIA-role chrome (`role="navigation"`, `role="banner"`, `role="contentinfo"`), and falls back to `role="main"` before `<body>` — halving boilerplate in research-phase fetches.
