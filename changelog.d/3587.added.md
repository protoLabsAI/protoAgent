- **ACP coder runs are traced in Langfuse (#3587).** Each coder run (the project board's coders included, which run outside any turn) is an `acp:<name>` agent span with its outcome, and every tool call the coder made is a child of it. Before this, no coder run reached Langfuse.
