- **Langfuse traces carry their input and output, and ACP coder runs are traced (#3587).** A turn's trace now shows the user's message and the agent's answer. Every generation shows the messages it was sent and its reply (capped at 8k chars per message; prompt capture keeps the full prompt). Traces group under Langfuse Sessions by session id. Before this, IO was left to the LiteLLM gateway, so an agent on a native OAuth provider logged no prompts or replies anywhere. Coder runs (the project board's included) are `acp:<name>` agent spans with each tool call nested under them; before this, no coder run reached Langfuse.
