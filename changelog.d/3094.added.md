- **Gource visualization scripts — render the repo's history as a growing contribution tree (#3094).**
  `scripts/render-gource.sh` produces a ~30s 1080p video (gource → ffmpeg, protoAgent
  lavender highlight) and `scripts/fetch-gource-avatars.sh` pulls contributor avatars;
  output stays local (`gource.mp4` / `.gource/` gitignored). Ported from protoMaker.
