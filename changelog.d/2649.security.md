- **Refreshed locked console dependencies with active advisories (#2649).**
  `mermaid`, `dompurify`, `postcss`, `nanoid`, and `undici` resolved to versions with
  known advisories in the shared lockfile. Live Mermaid rendering is off by default and
  Streamdown's sanitize/harden pipeline was never bypassed, so this wasn't an active
  exploit path — but Mermaid/DOMPurify still ship in the built console as a lazy asset,
  so it was worth closing. All five now resolve to patched versions within their existing
  semver ranges (`npm audit --omit dev` is clean). The remaining advisories are
  `esbuild`/`vite`/`vitepress` — development/docs tooling with no compatible fix short of
  a Vite major migration, left out of scope here.
