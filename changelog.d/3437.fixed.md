- **One plugin route can no longer take down `/openapi.json`.** A plugin page route annotated `-> HTMLResponse` with a function-local import (orgChart, portfolio, learning-wiki) made the schema, and `/docs`, answer 500 on every agent running it. The host now leaves such a route out of the schema with a warning naming it (it still serves), drops the cached schema when a plugin is disabled, and orgChart's page declares its response class.
