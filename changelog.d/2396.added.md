Agent-initiated plugin changes now push a live console refresh (ADR 0096 D8): the reload seam publishes `plugin.changed`, and a `PluginChangeWatch` subscriber refetches the plugin queries — so a rail view the agent just built and enabled appears immediately in every open console. Also gives the autoupdate loop's `plugin.updated` its first console consumer and unifies the Plugins-panel toggle on the shared refresh.
