`register_plugin_project` — graduate a self-built plugin to an ADR 0095 managed project (ADR 0096 D6): the registry's first runtime write path, scoped to the plugins dir, so fs tools address the plugin by name, the GitHub repo picker sees it, and a projectBoard can target it via `project_board.project`.
