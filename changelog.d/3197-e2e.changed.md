- **Fleet roster reordering is verified end-to-end (#3197).** The console E2E suite and the mock control-plane now exercise the accessible move-up/move-down reorder path against the persisted-order API (`PUT /api/fleet/order`): a reorder submits the complete immutable-id permutation (never editable names), a refetched roster renders in the saved order, a rejected reorder rolls back without dropping rows or breaking the non-reorder row actions, and discovered-network rows are held distinct from actual members.
