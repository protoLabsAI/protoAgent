- **Manage the fleet from the deck and the CLI (#3471).** In the deck: `n` creates a member from the hub's archetype catalog (inherit the hub's model connections, start after create), `R` renames (display name only; refresh moves to `F5`), `d` deletes with a typed-name confirm and a separate purge checkbox (a 409 "workspace survived" is shown as retryable), `a`/`e` add and edit remote members (bearer entered masked, sent once), `J`/`K` move a row and persist the roster order; the status line shows the warm-agent cap. `protoagent fleet` gains `new`, `rm`, `rename`, `remote add|edit|rm` and `order`, live through the hub or offline through the ops, with `--json`. Under the hood, `fleet.create`, `fleet.remove`, `fleet.rename`, `fleet.remotes.add|update|remove` and `fleet.order` join the `ops/` registry (listed by `protoagent operations` and `GET /api/operations`), carrying the orchestration `/api/fleet` used to hold inline — one implementation, three projections (ADR 0075 D2). Fifth slice of the fleet deck (epic #3466).
