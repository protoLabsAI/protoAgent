- **Retain rendered assistant replies across agent switches (#3340).** Switching to another agent and back could rehydrate a completed turn with only its tool-call cards — the rendered reply was dropped, because the transcript draws a parts-bearing bubble from its ordered parts and durable hydration did not always surface the answer as a trailing text run. Session hydration now reconciles the turn's server-joined answer text into the message's parts as that trailing run — mirroring the live reattach finalize — so the reply renders below the tool cards on return, with component/table/timeline output preserved alongside the recovered prose.
