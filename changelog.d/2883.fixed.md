- **Fix flaky Windows test for workflow run ordering (#2883).** Added a monotonic sequence tie-breaker to `WorkflowRunStore.recent()` and `paused()` so same-tick `updated_at` values sort deterministically.
