Plugin routers now hot-REMOUNT on every reload and unmount on disable — iterating on a plugin's console view goes live without a restart (previously the first-mounted route served stale forever, the #942 class the self-building loop's first live demo hit within an hour), and disabling a view/router plugin no longer recommends a restart.
