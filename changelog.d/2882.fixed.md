- **The artifact panel shows artifacts again (#2882).** Since v0.141.0 the panel booted
  dead — empty picker on the hub and every fleet member — because the shell's extracted
  `shell.js` was served behind auth that a `<script>` tag can never present. The script
  is now auth-exempt like the vendor files, and a guard test keeps every view subresource
  on the exempt list.
