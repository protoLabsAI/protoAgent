- **Docs plugin corpus missing from PyPI wheel (#2626).** `hatch_build.py::_SEEDS` never contained the `docs/` content sections, so a `pip install protoagent` install resolved `docs_root()` to a non-existent `site-packages/docs/` directory and the `docs_search`/`docs_read` tools + Docs view silently enumerated zero files. Added the five per-section entries (`docs/tutorials`, `docs/guides`, `docs/reference`, `docs/explanation`, `docs/adr`) matching `corpus.py::SECTIONS` and the existing `build_sidecar.py` pattern; `docs/dev/` (internal handoffs) and `.vitepress/` (build config) remain excluded.
