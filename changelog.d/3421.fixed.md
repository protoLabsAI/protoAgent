- Stop the OS-trust-store TLS tests flaking on Windows. Four "must fail closed" assertions
  demanded `httpx.ConnectError` specifically, but a handshake to a cert the trust store
  rejects can exceed the client timeout on that runner, and httpx then raises
  `ConnectTimeout` instead. The security property held the whole time — the assertion was
  narrower than the property it was checking.
