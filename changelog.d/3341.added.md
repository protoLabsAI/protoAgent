- **Paged effective-config reads (#3341).** `show_config` now accepts dotted nested selectors and explicit pagination for selected dict/list values and oversized strings, so large sections such as `project_board.projects` can be enumerated reproducibly without losing data to the transport cap.
