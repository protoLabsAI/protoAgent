- **fix(config): treat model name+provider as an atomic identity in the config cascade (#2622).** A fleet member that overrides only `model.name` (e.g. `model: {name: protolabs/smart}`) no longer inherits the host's `model.provider` via the per-field deep merge — the two keys only validate as a coherent pair, and mixing them produced `RuntimeError: model.provider is 'anthropic-oauth' but model.name='protolabs/smart' is not a Claude model id` on boot. `api_base` keeps per-field inheritance (an endpoint is not an identity). A member with no `model` section still inherits the host group wholesale.
