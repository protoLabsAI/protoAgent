- `protoagent fleet ls|up|down` now talk to the **running hub** (`live · <url>`) instead of reading `fleet.json` from whatever instance root the shell happened to resolve — beside the desktop hub the old output was a fabricated fleet of one with the CLI's own pid as the "running" host. A hub is found by evidence (`server.pid`, the `.instances/` heartbeats across every box root incl. the desktop app's, then `:7870`) and opened with its fleet service token; disk is a badged fallback (`offline`), `--json` is on every verb, and `--hub URL` / `--token` / `PROTOAGENT_HUB_TOKEN` reach a hub elsewhere. In live mode `up`/`down` go through the hub's control plane so it keeps owning its members. First slice of the fleet deck (#3467, epic #3466).
