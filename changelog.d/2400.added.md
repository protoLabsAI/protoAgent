The devkit ships a `self-building-demo` skill — the rehearsed script for demoing protoAgent building, testing, and hot-swapping its own plugins live, with the live-QA lessons (next-turn tool binding, the register() contract, state placement) baked into the beats.
