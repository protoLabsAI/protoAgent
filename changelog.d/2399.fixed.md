`test_plugin` now runs a plugin's suite against a disposable copy of the plugin dir — a destructive test (e.g. an empty-state test that deletes a data file, as a coding delegate wrote during ADR 0096 live QA) can no longer touch live files. The building-plugins skill now steers runtime state out of the plugin dir entirely.
